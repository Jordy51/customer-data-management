<!DOCTYPE html>
<html>
	<head>
		<title>Details</title>
		<link rel="stylesheet" type="text/css" href="/css/customerDetails.css" />
	</head>
	<body>
		<div class="container">
			<div class="heading">User Details</div>
			<div class="info">
				<table>
					<tbody>
						<% Object.keys(customer).forEach((field) => { %>
						<tr>
							<td class="label"><%= field %></td>
							<td colspan="2" class="value"><%= customer[field] %></td>
						</tr>
						<% }) %>
					</tbody>
				</table>
			</div>
			<a href="/customers/deleteCustomer/<%= customerId %>"><button>Delete</button></a>
		</div>
	</body>
</html>
